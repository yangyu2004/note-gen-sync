大语言模型（LLM）从 “通用预训练” 到 “对齐人类 / AI 偏好” 的核心微调技术，按技术演进顺序依次是：**SFT → FT → RLHF → RLAIF**

### 一、SFT（Supervised Fine-Tuning，监督微调）

**核心定义**：在预训练大模型基础上，用**人工标注的高质量 “指令 - 答案” 对**进行有监督训练。

* **目标**：让模型学会**理解并遵循人类指令**，具备基础对话、问答、任务执行能力。
* **本质**：**模仿学习**—— 模型学习 “标准答案”，拟合输入输出映射关系。
* **典型场景**：ChatGPT、Claude 等对话模型的第一步训练；垂直领域（医疗 / 法律）模型适配。
* **局限**：只能学 “正确”，无法区分 “更好 / 更差”；缺少负反馈与偏好排序。

### 二、FT（Fine-Tuning，微调）

**核心定义**：泛指在预训练模型上，用**特定任务 / 领域数据**进行的二次训练。

* **广义**：包含 SFT、全量微调、LoRA/QLoRA 等所有参数更新方式。
* **狭义**：常指**全量微调**（Full FT）—— 更新模型全部参数，成本高、易过拟合 / 遗忘。
* **与 SFT 的关系**：**SFT 是 FT 的一种**（监督式、指令导向的微调）。

### 三、RLHF（Reinforcement Learning from Human Feedback，基于人类反馈的强化学习）

**核心定义**：在 SFT 基础上，用**人类偏好反馈**做强化学习，让模型输出更符合人类价值观与偏好。

* **三步流程**：
  1. **SFT 基础**：先做监督微调，得到基础对话模型。
  2. **训练奖励模型（RM)**：人类标注员对模型生成的多个候选回答排序 / 打分，训练 RM 预测 “人类偏好分数”。
  3. **PPO 强化学习**：用 PPO 算法优化模型，让其生成**高分、更符合人类偏好**的回答。
* **目标**：从 “能做对” 到 “做得好、更安全、更有用、更对齐人类”。
* **典型场景**：ChatGPT、GPT-4、Claude 等主流对话模型的关键对齐技术。
* **局限**：**极度依赖人工标注**—— 成本高、速度慢、一致性难保证。

### 四、RLAIF（Reinforcement Learning from AI Feedback，基于 AI 反馈的强化学习）

**核心定义**：RLHF 的**低成本替代方案**—— 用**大模型（AI）替代人类**生成偏好反馈与奖励信号。

* **核心差异**：反馈来源从 “人” 变成 “AI”。
* **流程**：与 RLHF 几乎一致，仅将 “人类标注” 替换为 “AI 自动评估 / 排序”。
* **优势**：
  * **低成本、可规模化**：无需大量人工，反馈生成快、一致性高。
  * **性能接近 RLHF**：在多项任务上效果与 RLHF 相当甚至更好。
* **典型场景**：开源模型对齐、大规模多任务优化、降低对齐成本。
